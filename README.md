# 📚 Система управления книгами

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue.svg)](https://www.python.org/)
[![pytest](https://img.shields.io/badge/pytest-9.0.2-brightgreen.svg)](https://docs.pytest.org/)
[![Coverage](https://img.shields.io/badge/покрытие-%3E80%25-brightgreen.svg)](https://coverage.readthedocs.io/)
[![Ruff](https://img.shields.io/badge/code%20style-ruff-000000.svg)](https://docs.astral.sh/ruff/)
[![SOLID](https://img.shields.io/badge/SOLID-принципы-4c1.svg)](https://en.wikipedia.org/wiki/SOLID)

Мощная система управления коллекцией книг с консольным интерфейсом, 
разработанная на Python с соблюдением принципов SOLID и использованием современных 
паттернов проектирования. Проект демонстрирует чистую архитектуру (CLEAN ARCHITECTURE), 
высокую тестируемость и готовность к расширению.

## ✨ Особенности

- **📖 Полный CRUD** - Создание, чтение, обновление и удаление книг
- **🔍 Расширенный поиск** - Поиск по ID, названию, автору или ISBN с частичным совпадением
- **✅ Валидация данных** - Проверка формата ISBN, диапазона года издания (1450-текущий год)
- **💾 Постоянное хранение** - JSON-хранилище с автоматическим управлением файлами
- **🔄 Ленивая загрузка** - Данные загружаются только при необходимости
- **📊 Подробное логирование** - Детальное логирование для отладки и мониторинга
- **🧪 Высокое покрытие тестами** - Более 80% покрытия, 180+ тестов

🎯 Ключевые особенности

Валидация данных
 - Обязательные поля: Требует заполнения названия и автора
 - Год: Проверяет годы между 1450 (первая печатная книга) и текущим годом
 - ISBN: Принимает 10 или 13-значные ISBN, с дефисами/пробелами или без

Ленивая загрузка

Данные загружаются из JSON-файла только при первом обращении, что улучшает производительность и сокращает время запуска.

Возможности поиска
- Частичное совпадение: Поиск книг по части названия
- Нечувствительность к регистру: "CLEAN CODE" найдёт "Clean Code"
- Множественные критерии: Комбинация условий (автор + год)
- Поиск по ID: Точное совпадение UUID

Обработка ошибок
- Корректное восстановление после неверного ввода
- Понятные сообщения об ошибках для пользователя
- Поддержка отмены операций (введите 'cancel')
- Детальное логирование для отладки

🛠️ Используемые технологии

|Технология | Версия | Назначение |
|-----------|-----|------------|
|Python	|3.10+|Основной язык|
|pytest|9.0.2|Фреймворк тестирования|
|pytest-cov|7.0.0|Измерение покрытия кода|
|pytest-mock|3.15.1|Мокирование зависимостей|
|colorama|0.4.6|Цветной вывод в консоль|
|ruff|0.15.7|Линтер и форматтер кода|

## 🏗️ Архитектура проекта

### 📁 Структура проекта

    Book-Management-System/
    ├── src/
    │   ├── actions/                    # Действия (используется паттерн Command)
    │   │   ├── base_action.py            # Абстрактный базовый класс действия
    │   │   ├── entity_service_action.py  # Базовый класс для действий с сущностями
    │   │   ├── add_entity_action.py      # Добавление книги
    │   │   ├── delete_entity_action.py   # Удаление книги
    │   │   ├── list_entities_action.py   # Список всех книг
    │   │   ├── search_entity_action.py   # Поиск книг
    │   │   ├── update_entity_action.py   # Обновление книги
    │   │   └── exit_action.py            # Выход из приложения
    │   ├── managers/                   # Оркестрация приложения
    │   │   ├── base_manager.py           # Абстрактный базовый класс контроллера приложения
    │   │   └── book_manager.py           # Главный контроллер приложения
    │   ├── models/                     # Модели данных
    │   │   ├── base_entity.py            # Базовая сущность с UUID
    │   │   └── book.py                   # Модель книги
    │   ├── services/                   # Слой бизнес-логики
    │   │   ├── base_service.py
    │   │   ├── entity_service.py         # Обобщенные CRUD операции
    │   │   └── validation_service.py     # Валидация ISBN, года
    │   ├── storage/                    # Хранилище (репозиторий)
    │   │   ├── base_storage.py
    │   │   └── json_storage.py           # JSON-хранилище с поддержкой UUID
    │   ├── exceptions.py               # Иерархия исключений
    │   ├── settings.py                 # Конфигурационные константы
    │   └── main.py                     # Точка входа в приложение
    ├── tests/                          # Набор тестов
    ├── pyproject.toml               # Конфигурация Ruff
    ├── pytest.ini                   # Конфигурация pytest
    ├── requirements.txt             # Зависимости
    └── README.md                    # Этот файл



## Код проекта разрабатывался с соблюдением принципов SOLID:

1. Принцип единственной ответственности (SRP, Single Responsibility Principle) - Все классы проекта отвечают за что-то одно:
    - actions - только за действия (добавить, удалить и т.д.)
    - managers - только для запуска
    - models - только для хранения данных
    - servicies - занимается управлением сущностями (получение по id, добавление, удаление и т.д.)
    - storage - взаимодействие с БД (json-файл), т.е. загрузка и сохранение данных
2. Принцип открытости-закрытости (OCP, Open/Close Principle) - добавление новых действий для Book Manager это просто добавление новых actions (снаружи), логика работы самого менеджера не меняется.
3. Принцип подстановки Барбары Лисков (LSP, Liskov Substitution Principle) - наследники (например AddEntityAction, наследуемый от EntityServiceAction, который в свою очередь наследуется от Action) подходят там же, где подходит базовый класс.
4. Принцип разделения интерфейсов (ISP, Interface Segregation Principle) - в Book Manager не объединяются вместе JsonStorage и EntityService, они разделены, это разные классы. 
JsonStorage взаимодействует с БД, а EntityService не работает с БД напрямую, а работает только с кэшем. 
5. Принцип инверсии зависимостей (DIP, Dependency Inversion Principle) - абстрактный класс BaseService, от которого наследуется EntityService, ничего в себя не принимает.
EntityService ожидает BaseStorage (абстрактный класс), но сам BaseStorage никого не ждет. 
Затем в  EntityService "прокидывается" JsonStorage. Т.о. реализации зависят от абстракций, а не абстракции от реализаций.

## Документация классов, методов и функций

Все файлы проекта имеют полную документацию в формате docstring. Документация включает:

* Описание назначения каждого класса
* Описание всех методов с их параметрами и возвращаемыми значениями
* Указание на возможные исключения
* Примеры где это уместно
* Документирование атрибутов классов

Это значительно улучшает читаемость кода и помогает другим разработчикам быстро понять структуру и назначение каждого компонента системы.

## Тестирование

### 📊 Статистика:

- Всего тестов: 181
- Пройдено: 181 ✅
- Покрытие кода: >80% (включая все основные компоненты)

### 📁 Структура тестов:
    tests/
    ├── conftest.py                          # Общие фикстуры
    ├── unit_tests/                          # Модульные тесты
    │   ├── test_models.py                   # Модели данных (BaseEntity, Book)
    │   ├── test_exceptions.py               # Иерархия исключений
    │   ├── test_validation_service.py       # Валидация ISBN, года, полей
    │   ├── test_json_storage.py             # JSON хранилище
    │   ├── test_entity_service.py           # CRUD операции
    │   └── test_actions.py                  # Действия пользователя
    └── integration_tests/                   # Интеграционные тесты
        ├── test_storage_integration.py      # Работа с файловой системой
        └── test_book_manager_integration.py # Полный цикл работы приложения

### ✅ Что тестируется:

1. Модели - создание, наследование, UUID
2. Исключения - иерархия, атрибуты, сообщения
3. Валидация - ISBN, год публикации, обязательные поля
4. Хранилище - чтение/запись JSON, обработка ошибок, Unicode
5. Сервисы - CRUD операции, поиск, обновление, ленивая загрузка
6. Действия - добавление, удаление, поиск, обновление, выход
7. Менеджер - инициализация, меню, выбор действий, цикл приложения
8. Интеграция - полный цикл работы с реальной файловой системой

### 🛠️ Использованные инструменты:
- pytest - основной фреймворк тестирования
- pytest-mock - мокирование зависимостей
- pytest-cov - измерение покрытия кода
- unittest.mock - дополнительные моки

### 🚀 Запуск тестов:
```bash
pytest -v                    # Подробный вывод
pytest --cov=src --cov-report=html  # Отчет о покрытии (создастся каталог html)
```
### 📈 Покрытие кода:
```bash
pytest --cov=src --cov-report=html --cov-report=term
```
либо с использованием **coverage**:
```bash
coverage run -m pytest   
coverage report -m 
```

